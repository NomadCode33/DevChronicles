/*
Write a function which converts the input string to uppercase.

function makeUpperCase(str) {
  // Code here
}
*/

// Answer:
// The answer was simple, I appended the .toUpperCase() to the end of the
// string to capitalize it entirely
function makeUpperCase(str) {
  return str.toUpperCase();
}